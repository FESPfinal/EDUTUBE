'use client';

const LoadingRequest = () => {
  return (
    <div className=" inset-0 w-full h-screen z-50 flex fixed items-center justify-center">
      <p>요청 중입니다...</p>
    </div>
  );
};

export default LoadingRequest;
